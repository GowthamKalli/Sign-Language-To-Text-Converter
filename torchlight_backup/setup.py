from setuptools import find_packages, setup

setup(
    name='torchlight',
    version='1.0',
    description='A mini framework for pytorch',
    packages=['torchlight'],
    package_dir={'torchlight': 'torchlight'},
    install_requires=[])
